# Local overrides — copy from config.local.env.example and customize.
# This file is gitignored.

CONTEXT=local
BASE_URL=
HEADLESS=false
BROWSER=chromium
TIMEOUT_MS=30000
SLOW_MO_MS=0
VIEWPORT_WIDTH=1440
VIEWPORT_HEIGHT=900
TRACE=retain-on-failure
SCREENSHOT=only-on-failure
VIDEO=off
PARALLEL_WORKERS=auto
RERUNS=0
